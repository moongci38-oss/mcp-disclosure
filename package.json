{
  "name": "agenttrust",
  "version": "0.1.0",
  "description": "Local-only configuration disclosure CLI for AI agent/MCP setups",
  "type": "module",
  "bin": { "agenttrust": "./bin/agenttrust.js" },
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "npm run build",
    "test": "node --test dist/test/**/*.test.js"
  },
  "dependencies": { "js-yaml": "^4.1.0" },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "@types/js-yaml": "^4.0.9"
  },
  "license": "MIT",
  "agenttrust": {
    "supportedScannerRange": "0.x"
  }
}
